# 루틴 실행기 제작

## 프로젝트 개요

- **무엇**: 루틴을 **한 번에 한 항목씩** 크게 띄우고, 완료를 누르면 소리와 함께
  다음으로 넘어가는 단일 HTML 파일. 루틴은 **자주 하는 것**(시작 화면의 큰 버튼)과
  **가끔 하는 것**(`☰` 메뉴 안) 두 층으로 나눠 둔다 (D-023)
- **형태**: 의존성 없는 단일 HTML 파일 (`index.html`). 빌드 없음, 서버 없음
- **왜**: 루틴을 목록으로 보면 전체가 눈에 들어와서 압박이 되고, 어디까지 했는지 계속
  다시 세게 된다. **지금 할 것 하나만 보이면** 그 하나만 하면 된다
- **사용자**: 본인 1인. 맥에서 시작해 나중에 안드로이드로 확장 (D-001)
- **상태**: `docs/STATUS.md` 참조 — 여기에 상태를 적어 두면 반드시 낡는다

## 폴더 구조

**프로젝트 위치**: `~/Projects/루틴 실행기 제작`. 모든 프로젝트는 `~/Projects/` 아래에 둔다
(**대문자 `P`**. macOS 는 대소문자를 안 가려 `~/projects` 로도 열리지만, 같은 저장소를
두 이름으로 부르면 git 출력이 엇갈려 보인다).

```
루틴 실행기 제작/
├── CLAUDE.md              ← 이 문서. 개요와 일하는 방식
├── index.html             ★ 본체. 이 파일 하나가 앱 전부다
├── manifest.webmanifest   홈 화면에 추가했을 때 앱처럼 뜨게 하는 설정.
│                          아이콘 주소의 `?v=` 지문은 손으로 고치지 않는다 (D-029)
├── sw.js                  오프라인 실행 (서비스 워커)
├── icon-192.png           홈 화면 · 시작 화면 · 파비콘 (꽉 채움. 잘릴 몫을 빼고 그린다 — D-030)
├── icon-512.png
├── icon-inset-192.png     자르지 않고 그리는 자리용 (여백 + 둥근 모서리 — D-028)
├── icon-inset-512.png
├── .nojekyll              GitHub Pages 가 Jekyll 로 건드리지 않게
├── mac/
│   ├── Routine.swift      맥앱 소스 (WKWebView + 클로드코드 다리)
│   ├── icon-mac-1024.png  맥 전용 아이콘 (여백 + 둥근 모서리 — D-019)
│   └── build.noindex/     빌드 산출물 (routine.app) — git 추적 안 함
│                          .noindex 라 Spotlight·Alfred 가 색인하지 않는다 (D-021)
├── tools/
│   ├── check.sh           회귀 검사
│   ├── make-icons.py      아이콘 생성 + 매니페스트에 지문 찍기 (D-029)
│   ├── build-mac-app.sh   맥앱 빌드 (--install 로 /Applications 에 설치)
│   ├── push-send.py       정해진 시각 알림을 보낸다 — 맥에서 돈다 (D-033).
│   │                      비밀키·등록 정보는 `~/.routine-push/` 에만 둔다
│   └── push-setup.sh      그것을 launchd 에 건다 (7:30 · 20:30)
└── docs/
    ├── STATUS.md          ★ 지금 상태 — 세션을 시작하면 이것부터 읽는다
    ├── UPDATES.md         갱신 이력 (언제 무엇이 바뀌었나 — 버전마다 한 칸)
    ├── INDEX.md           문서 지도 (어떤 질문에 어떤 문서가 답하는지)
    ├── DECISIONS.md       결정 장부 (왜 이렇게 했나)
    ├── BACKLOG.md         다음에 할 일 (무엇을 어떤 순서로)
    └── SPEC.md            동작 규격 (무엇이 어떻게 동작해야 하는가)
```

## 일하는 방식 — 반드시 지킨다

1. **실제로 확인되기 전까지 "된다"고 하지 않는다.**
   이 프로젝트에서 '진짜 확인'은 **실제 기기의 실제 브라우저에서 손으로 눌러 본 것**이다.
   코드를 읽어서 맞아 보이는 것, 데스크톱 브라우저의 모바일 시뮬레이터에서 되는 것은
   확인이 아니다. 특히 이 셋은 시뮬레이터로 절대 판정할 수 없다:
   - **소리가 실제로 나는가** (기기 볼륨·무음 모드·자동재생 정책에 걸린다)
   - **터치가 실제로 편한가** (버튼 크기, 손가락 위치, 오작동)
   - **화면이 안 꺼지는가** (Wake Lock 은 실기기에서만 판정된다)

   검증 상태는 ✅검증(방법·날짜 포함) / ⚠️미검증 / ❌불가 로 구분해 적는다.
2. **제1 원칙: 한 화면에 한 항목.**
   진행바·타이머·건너뛰기 같은 보조 요소는 있어도 되지만, **다음 항목이 무엇인지 미리
   보이면 안 된다.** 이 앱의 존재 이유가 그것이기 때문이다. 기능을 넣을지 말지 애매하면
   이 원칙으로 판단한다.
3. **의존성 최소 — 단일 파일을 깬다면 넣지 않는다.**
   프레임워크·빌드 도구·패키지 매니저·CDN·웹폰트·외부 이미지·오디오 파일 전부 없다.
   `index.html` 하나를 어디에 두고 더블클릭해도 완전히 동작해야 한다 (D-002).
   매니페스트·서비스워커는 `https` 에서만 붙는 선택적 껍데기이고, 없어도 앱은 온전하다.
   - 특히 **외부 파일을 `fetch` 로 읽지 않는다** — `file://` 에서 CORS 로 막힌다
4. **부분 수정 원칙.** 전체 재작성 대신 해당 부분만 고친다.
   특히 **`ROUTINES` 기본 목록**과 **손으로 맞춘 CSS 값**은 임의로 갈아엎지 않는다.
5. **핵심 규칙을 바꾸면 결정 장부에 남긴다.** `docs/DECISIONS.md` 에 번호·날짜·근거를 적는다.
6. **회귀 확인은 `tools/check.sh` 로 한다.** (본체가 돌기 시작한 뒤에 만든다 — B-06)
   핵심 로직을 건드렸으면 반드시 돌리고, 결과(통과/실패 개수)를 보고에 적는다.
   - 실패하면 둘 중 하나다 — 로직을 되돌리거나, **의도한 변경이면** 결정 장부에 근거를
     남기고 검사 쪽을 고친다. **검사만 조용히 고치지 않는다**
   - **이 검사가 못 하는 것**: 소리, 터치감, Wake Lock, 실제 화면 크기에서의 레이아웃.
     즉 위 1번의 세 가지는 영원히 손으로 확인해야 한다
7. **동작하는 지점마다 자동 커밋.** 이 폴더는 git 저장소다
   (`main` → `github.com/powerprana7/morning-evening-routine`).
   동작을 확인한 상태가 되면 **따로 묻지 않고 커밋**해 되돌아갈 지점을 남긴다.
   깨진 상태로 쌓아두지 않는다.
   - 커밋 메시지는 한국어로, 무엇을 왜 바꿨는지 한 줄 요약 + 필요하면 본문
   - **되돌리기·이력 삭제(`reset --hard`, 브랜치 삭제 등)는 반드시 먼저 묻는다**
   - **커밋했으면 push까지 한다 — 묻지 않는다** (2026-08-28 지시, D-026).
     커밋만 하고 멈추면 폰과 맥앱에는 아무것도 도착하지 않는다. 이 앱은 여러 기기에서
     같은 것을 보는 것이 전제라, 올라가지 않은 커밋은 **끝나지 않은 작업**이다
   - ⚠️ **이 저장소만 Public이다.** GitHub Pages 무료 플랜은 Private 저장소를 게시하지
     못해서, Private으로 돌리면 맥앱과 안드로이드가 즉시 끊긴다.
     그래서 **push가 곧 인터넷 공개**이고 커밋에 박힌 이메일도 함께 영구 공개된다.
     자동으로 올린다는 것은 이것을 알고 올린다는 뜻이다 — **공개되면 안 되는 것을
     애초에 이 저장소에 커밋하지 않는다**가 그 대가로 지켜야 할 규칙이다.
     비밀번호·토큰·개인정보가 섞일 낌새가 있으면 커밋 전에 멈추고 묻는다
8. **버전 = `날짜.번호`. 동작이 바뀌면 반드시 올린다. 올렸으면 `docs/UPDATES.md` 에
   칸을 만든다.**
   `index.html` 안의 `const VERSION = 'YYYY-MM-DD.N'` 이 유일한 출처이고, 시작 화면
   구석에 그대로 표시된다. 여러 기기에 파일을 복사해 쓰게 되므로 **지금 열고 있는 게
   최신인지 판단할 방법이 이것뿐**이다. 동작이 바뀌는 커밋에서는 이 값을 올리는 것이
   커밋의 일부다. 문서만 고친 커밋은 올리지 않는다.
   - **번호는 그날의 몇 번째 판인가.** 1부터 세고 날짜가 바뀌면 다시 1로 간다.
     하루에 한 번만 바꿔도 `.1` 을 붙인다 — 맨 날짜와 섞이면 구분이 안 된다 (D-032)
   - **하루에 여러 번 바꾸면서 번호를 안 올리면 기기 도달 여부를 못 가린다.**
     2026-08-28 과 09-04 에 실제로 걸렸다. 그래서 번호를 붙였다
9. **여러 날에 걸쳐 이어서 한다 — 상태를 문서에 남긴다.**
   - **세션을 시작하면 `docs/STATUS.md` 를 먼저 읽는다.** git log 를 뒤져 상태를
     재구성하지 않는다 (그렇게 하면 틀린다)
   - **동작이 바뀌는 커밋에는 `docs/STATUS.md` 갱신을 포함한다.** 버전 날짜와 같은 취급
   - **버전을 올렸으면 `docs/UPDATES.md` 에 그 버전의 칸을 만든다.** STATUS 는 지난
     상태를 덮어쓰므로, 이력이 남는 곳은 여기뿐이다 (D-023)
   - **작업은 `docs/BACKLOG.md` 에 등록하고 시작한다.** 번호(`B-xx`)를 커밋 메시지와
     결정 장부에서 그대로 참조한다

## 빠른 참조

```bash
PROJ=~/Projects/"루틴 실행기 제작"

open "$PROJ/index.html"                       # 맥에서 실행 (기본 브라우저)
open -a "Google Chrome" "$PROJ/index.html"    # 브라우저 지정해 실행
grep -n "const VERSION" "$PROJ/index.html"    # 현재 버전 확인
grep -n "tier:" "$PROJ/index.html"            # 루틴 목록과 배치 층 한눈에
"$PROJ/tools/check.sh"                        # 회귀 검사
"$PROJ/tools/build-mac-app.sh" --install      # 맥앱 빌드 + 설치

"$PROJ/tools/push-setup.sh"                   # 알림 설치·갱신 (몇 번을 돌려도 안전)
~/.routine-push/push-send.py status           # 알림 상태 — 등록됐나, 최근 기록
~/.routine-push/push-send.py send --force     # 지금 바로 보내 확인
```

**알림에서 절대 하면 안 되는 것** (D-033)

- **비밀키를 저장소에 넣지 않는다.** 이 저장소만 Public 이다. `index.html` 의
  `VAPID_PUBLIC` 은 공개되라고 있는 값이라 괜찮고, 짝인 비밀키는 맥에만 둔다
- **열쇠를 다시 만들지 않는다.** 폰에 등록된 알림 주소가 전부 못 쓰게 된다
- **보낼 시각을 한 파일만 고치지 않는다.** `push-send.py` 의 `SCHEDULE` 과
  `push-setup.sh` 의 `StartCalendarInterval` 이 짝이다 (검사 12번이 붙잡는다)
