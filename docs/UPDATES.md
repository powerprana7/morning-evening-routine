# 갱신 이력 (UPDATES)

**이 문서가 답하는 질문**: *언제 무엇이 바뀌었나.*

버전은 날짜다(`index.html` 의 `VERSION`, `CLAUDE.md` 8번). 여기 **한 버전에 한 칸**씩,
**새것이 위로** 쌓는다. 이미 적은 칸은 고치지 않는다 — 틀렸으면 다음 칸에서 바로잡는다.

**다른 문서와의 경계**

| | |
|---|---|
| `STATUS.md` | **지금** 뭐가 되나 — 늘 현재 시점 하나만 있다 |
| `UPDATES.md` | **언제** 뭐가 바뀌었나 — 지난 것을 지우지 않고 쌓는다 (이 문서) |
| `DECISIONS.md` | **왜** 그렇게 정했나 — 근거와 버린 대안 |
| `BACKLOG.md` | **다음에** 뭘 하나 |

한 줄로 줄이면: 여기는 **바뀐 것**, 결정 장부는 **바꾼 이유**다.
그래서 이 문서의 각 칸은 결정 장부와 할 일 번호(`D-0xx` · `B-xx`)를 가리키기만 하고,
근거를 여기에 옮겨 적지 않는다.

**한 칸에 적는 것**: 바뀐 것 · 안 바뀐 것 · 확인한 것과 못 한 것.
마지막 항목이 특히 중요하다 — 이 프로젝트에서 "된다"는 **손으로 눌러 본 것**만이다
(`CLAUDE.md` 1번).

---

## 2026-09-04.5 — 정해진 시각 알림. 7:30 과 20:30

**한 줄**: 폰이 **7시 30분과 20시 30분에 알림을 받는다.** 누르면 그 루틴이 바로
열린다. 보내는 것은 맥이다. (B-39, D-033)

**D-009 를 뒤집은 칸이다.** 그 결정은 "정해진 시각에 먼저 알려주기"만이 형식 선택을
뒤집을 조건이라고 적어 두고 *"원하게 되면 이 항목을 뒤집는 것부터 시작한다"* 로
닫혀 있었다. 형식(HTML)은 그대로 두고 웹 푸시만 얹었다.

### 바뀐 것

| 파일 | 무엇 |
|---|---|
| `index.html` | 시작 화면에 `⏰ 알림` 단추. 누르면 알림 주소를 만들어 **맥에 붙여넣을 명령**을 클립보드에 넣는다. `?routine=` 으로 들어오면 그 루틴을 연다 |
| `sw.js` | `push` 로 알림을 띄우고 `notificationclick` 으로 루틴을 연다. 아이콘 이름은 **매니페스트에서 읽는다** (D-029 를 깨지 않으려고) |
| `tools/push-send.py` | 새 파일. 맥에서 신호를 보낸다. 의존성은 `cryptography` 하나 |
| `tools/push-setup.sh` | 새 파일. 열쇠를 만들고 launchd 에 7:30 · 20:30 을 건다 |
| `tools/check.sh` | 검사 12번 추가 |

**빈 신호만 보낸다.** 문구는 `sw.js` 안에 있다 — 암호화 단계가 없어지고, push 한 번
이면 문구가 전 기기에 반영된다. 어느 루틴인지는 받은 시각으로 가린다.

### 안 바뀐 것

- **루틴 내용은 한 글자도 안 건드렸다** — 모닝 27 / 이브닝 28 / 수련과 운동 7 / 출국 전 41
- 실행·편집·요약 흐름, 소리, 뷰 방식, 화면 톤 전부 그대로
- `file://` 과 맥앱에서는 알림 단추가 **안 보인다.** `https` 가 아니면 감춘다 —
  알림이 없어도 앱은 온전하다 (CLAUDE.md 3번)

### 확인한 것

| | |
|---|---|
| 회귀 검사 | ✅ 210건 + 9번 + **새 12번** 통과 |
| 검사 12번이 진짜 잡는가 | ✅ 시각을 어긋나게, `push` 처리를 지워서 **각각 실패하는 것까지 확인** |
| 서명(VAPID) 이 규격대로인가 | ✅ JWT 를 되풀어 검증 — 서명 64바이트·공개키 65바이트(`0x04`)·`aud`·만료. 앱에 박힌 공개키와 맥의 비밀키가 짝이 맞는 것도 확인 |
| 시각 판정 | ✅ 9가지 시각으로 확인 (`07:29` 안 보냄 / `07:30`·`08:00` 보냄 / `08:01` 안 보냄 / `13:00` 안 보냄) |
| 실패가 시끄러운가 | ✅ 등록 없이 보내니 맥 알림이 떴다 |
| launchd 등록 | ✅ `launchctl` 목록에 있음, 07:30 · 20:30 |

### 같은 날 밤에 폰에서 판정했다 (21:56~)

- ✅ 폰에서 `⏰ 알림` → 알림 주소가 나오고 맥에 등록됨
- ⚠→✅ **첫 발송이 `CERTIFICATE_VERIFY_FAILED` 로 막혔다.** python.org 판 파이썬에
  인증서가 0개였다. `ssl_context()` 로 고치고 `201` 을 받았다.
  **실패 알림(`shout()`) 덕에 바로 드러났다** — 이 설계가 값을 한 첫 사례다
- ✅ **폰에 알림이 떴다**
- ✅ **알림을 누르니 이브닝 루틴이 바로 열렸다**
- ✅ **소리도 울렸다** — 다만 **세 번째 발송에서야** 났다. 폰 설정은 내내
  `Allow sound and vibration` 이었으니 설정 문제가 아니었다. 첫 두 번이 조용했던
  이유는 못 갈랐다. 유력한 것은 **알림창에 같은 표식의 알림이 남아 있어
  갈아끼워진 것** — B-40 으로 열어 뒀다

**이 칸으로 B-39 의 판정이 끝났다.** 남은 것은 2026-09-05 07:30 첫 자동 발송뿐이다.

### 확인 못 한 것
- **시작 화면 단추가 셋으로 늘어난 뒤의 레이아웃.** 폭 360px 에서 가로로 안 들어가는
  것을 계산으로 짚어 `flex-wrap` 을 넣었지만, 실기 확인은 못 했다.
  이 맥의 GUI 자동화가 막혀 있어 브라우저 화면을 볼 수 없었다

---

## 2026-09-04.4 — 모닝 순서 하나. 폰에서 고쳐 보낸 첫 반영

**한 줄**: `창문을 열고 환기를 시킨다` 가 **4번에서 7번으로** 내려갔다. 옮긴 것은 이
한 줄뿐이고 27개는 그대로다. (B-38)

**이 칸이 B-37 파이프라인의 실사용 1호다** — 폰의 편집 화면에서 순서를 고쳐 지시문을
받아 반영했다.

### 바뀐 것

| | 4 | 5 | 6 | 7 |
|---|---|---|---|---|
| 전 | **창문을 열고 환기** | Make bed | 천부경, 수진선학 훈 | 음파축기공 |
| 후 | Make bed | 천부경, 수진선학 훈 | 음파축기공 | **창문을 열고 환기** |

### 안 바뀐 것

나머지 26개의 이름·설명·알림 시각. `오일풀링 @10` 과 음파축기공 `@5` 도 그대로다.
`Reflenesh your nutrition` 오타는 **손대지 않았다** — 주신 목록 그대로다 (B-17 은 계속 대기).

### 확인한 것과 못 한 것

| | |
|---|---|
| ✅ 27개가 주신 목록과 **글자까지 일치** | 자모 정규화(NFC)를 맞춰 한 줄씩 대조 |
| ✅ 회귀 검사 210건 통과 | 왕복 변환·중복 없음 포함 |
| ✅ 코드 변경이 한 줄 이동뿐 | `git diff` — 옮긴 줄 하나 + 버전 |
| ⚠️ 폰에서 새 순서로 돌려봤는가 | 미검증 |

---

## 2026-09-04.3 — 버전에 번호가 붙는다

**한 줄**: 하루에 여러 번 바꾸면 전부 같은 이름이 되어 **폰에 새 파일이 갔는지를
화면으로 못 가렸다.** 두 번 걸린 뒤에 고쳤다. (D-032 · B-35)

**이 칸부터 제목이 `날짜.번호`다.** 위의 `2026-09-04` 와 `2026-09-04 (2)` 를
`.1`·`.2` 로 읽으면 이어진다 — 이미 적은 칸은 고치지 않는다.

### 바뀐 것

**1. `VERSION` 이 `YYYY-MM-DD.N` 꼴** — N 은 그날의 몇 번째 판. 1부터 세고 날짜가
바뀌면 다시 1. **하루 한 판이어도 `.1` 을 붙인다** — 맨 날짜와 섞이면 "하나뿐이라
번호가 없는 것"인지 "옛 판"인지 구분되지 않는다.

**2. `CLAUDE.md` 8번** — "버전 = 날짜" → "버전 = `날짜.번호`".

**3. 회귀 검사 11번** — 207건 → **210건**. 맨 날짜로 되돌아가면 실패한다.

### 안 바뀐 것

앱 동작. 시작 화면 구석의 글자만 `2026-09-04` → `2026-09-04.3` 으로 길어진다.

### 확인한 것과 못 한 것

| | |
|---|---|
| ✅ 회귀 검사 210건 통과 | |
| ✅ 새 검사가 문다 | 맨 날짜로 되돌리니 `버전이 날짜.번호 꼴` 실패 |
| ⚠️ **검사는 꼴만 본다** | "지난번보다 올랐는가"는 못 본다 — `.3` 을 두 번 내보내도 통과한다 |

---

## 2026-09-04 (2) — 공유 시트를 뺐다. 챗은 맥에 닿지 못한다

**한 줄**: 같은 날 (1) 이 **틀린 곳으로 데려갔다.** 공유 시트로 넘긴 지시문은 새 챗에
떨어졌고, 그 챗은 맥을 건드리지 못한 채 **"완료했다"고 답했다.** 공유를 빼고 복사 +
`Code` 화면 열기로 바꿨다. (D-031 개정 · B-37)

### 무슨 일이 있었나

공유 시트에 클로드가 떴고, 골랐더니 **새 챗**이 열려 지시문을 받았다. 그 챗은
`/tmp/morning-evening-routine` 에 저장소를 **새로 받아** 고치고 커밋(`6dffd77`)한 뒤
`✓ 완료` 표를 그렸다. Push 만 `⚠ 수동 필요`.

**실제로는 아무것도 반영되지 않았다.** 맥도 Dropbox 도 관여하지 않았고, 그 커밋은
샌드박스와 함께 사라진다. 고친 순서는 어디에도 남지 않았다.

조용히 실패한 것이 아니라 **성공했다고 말하면서 실패했다.** D-017 때와 같은 모양인데
이번이 더 나쁘다 — 화면에 체크가 줄줄이 찍히기 때문이다.

### 바뀐 것

**1. 폰에서 `navigator.share` 를 뺐다.** 대신 ① 클립보드에 넣고 ②
`https://claude.ai/code` 를 **새 창으로** 연다. 거기서 맥 세션을 골라 붙여넣는다.

| 기기 | 누르면 | (1) 에서 바뀜 |
|---|---|---|
| 맥앱 | 터미널이 열리며 `claude` 실행 | — |
| **폰** | **복사 + `Code` 화면 열기** | **공유 시트 → 이것으로** |
| 데스크톱 브라우저 | 클립보드 복사 | — |

**2. 지시문에 못을 박았다** — 목적지를 잘못 골라도 **받는 쪽이 스스로 멈추게** 한다.

```
⚠ push 까지 끝나야 반영이다. 임시 폴더에 새로 clone 해서 고치면 아무 데도 안 남는다.
push 를 못 하겠으면 커밋해 놓고 "완료"라고 하지 말고, 안 됐다고 분명히 말해줘.
```

**3. 편집 화면 안내** — "새 챗에 붙여넣으면 반영되지 않는다"를 명시했다.

**4. 회귀 검사** — 203건 → **207건**. 공유 시트로 되돌아가는 것, 여는 곳이 챗으로
바뀌는 것, 복사/열기 순서가 뒤집히는 것, 같은 창으로 여는 것, 지시문의 못이 빠지는
것을 각각 붙잡는다.

### 안 바뀐 것

- 편집 진입 개방 — (1) 의 그 부분은 **실기에서 확인됐다**
- 클립보드가 늘 **먼저** 온다 (초점을 잃으면 그 뒤로 쓰기가 막힌다)
- 맥앱 직통(D-020) · 데스크톱 복사 · 앱은 루틴을 저장하지 않는다(D-015)

### 확인한 것과 못 한 것

| | |
|---|---|
| ✅ **안드로이드에서 편집 진입이 뜬다** | 사용자 스크린샷 (2026-09-04) — 탭 4개 |
| ✅ **지시문이 온전히 실린다** | 공유 미리보기 첫 줄이 정확했다 |
| ✅ **공유는 챗으로 간다 (그래서 뺐다)** | 사용자 스크린샷 — Haiku 4.5 챗, `/tmp` 샌드박스 |
| ✅ 회귀 검사 207건 통과 | 새 검사 6건이 무는 것까지 확인 |
| ✅ 네 갈래가 의도대로 갈린다 | `copyEditor` 스텁 — 맥앱/폰 복사 성공/폰 클립보드 막힘/데스크톱. 복사가 늘 창 열기보다 먼저 |
| ⚠️ **`claude.ai/code` 를 앱이 가져가는가** | **실기 미검증.** 브라우저로 새도 복사는 이미 끝나 있다 |

---

## 2026-09-04 — 폰에서도 루틴 순서를 고쳐 보낼 수 있다

**한 줄**: 안드로이드 클로드 앱이 맥의 클로드코드에 원격으로 붙게 되면서, **"편집은
맥에서만"의 근거가 사라졌다.** 감춰 뒀던 편집 진입을 열고, 폰에서는 공유 시트로
클로드 앱에 넘긴다. (D-031 · B-37)

### 바뀐 것

**1. 안드로이드에서 `✎ 루틴 편집` 이 보인다** — `pointer: fine` 이 아니면 버튼을
지우던 게이트를 없앴다. 루틴 넷 다 맥과 똑같이 고칠 수 있다.

**2. 전달 경로가 셋으로 갈린다**

| 기기 | 버튼 딱지 | 누르면 |
|---|---|---|
| 맥앱 | `클로드코드로 보내기` | 터미널이 열리며 `claude` 실행 (전과 같음) |
| **폰** | **`클로드코드로 보내기`** | **공유 시트 → 클로드 앱** (새로 생긴 길) |
| 데스크톱 브라우저 | `전체 복사` | 클립보드 복사 (전과 같음) |

**3. 공유 경로에서도 클립보드에 먼저 넣는다** — 공유 시트가 뜨면 문서가 초점을 잃어
그 뒤로는 클립보드 쓰기가 막히고, 취소했을 때 붙여넣을 것이 남지 않는다. 순서가
뒤바뀌어도 맥에서는 아무 증상이 없어서 회귀 검사가 순서를 글자 그대로 붙잡는다.

**4. 편집 화면 안내 문구** — "브라우저에서는 복사만 된다" → 폰과 데스크톱을 나눠 적었다.

**5. 회귀 검사 10번** — 197건 → **203건**. 진입 버튼을 다시 감추거나, 공유 호출이
빠지거나, 복사와 공유의 순서가 뒤집히거나, 맥앱 직통 경로가 지워지면 걸린다.

### 안 바뀐 것

- **지시문 내용.** D-022 에서 이미 GitHub 저장소를 가리키게 바꿔 둬서 어느 기기에서
  보내도 같은 곳을 가리킨다 — 이번 변경이 가능했던 것이 그 덕이다
- **앱은 여전히 루틴을 저장하지 않는다** (D-015 의 핵심). 편집 화면은 고쳐서 보내는
  곳이지 이 기기에 남기는 곳이 아니다. 기기별 사본이 갈라지는 문제는 그대로 막혀 있다
- 맥앱 직통 경로(D-020) · 루틴 목록 · 화면 톤 · 뷰 방식

### 확인한 것과 못 한 것

| | |
|---|---|
| ✅ 회귀 검사 203건 통과 | `tools/check.sh` |
| ✅ 새 검사가 실제로 문다 | 옛 게이트를 되살리고 복사/공유 순서를 뒤집어 보니 2건 실패 |
| ✅ 네 갈래가 의도대로 갈린다 | `copyEditor` 를 떼어내 스텁으로 돌렸다 — 맥앱/공유 성공/공유 취소/데스크톱. 클립보드가 늘 공유보다 먼저 |
| ⚠️ **클로드 앱이 공유 대상으로 뜨는가** | **실기 미검증.** 안 뜨면 복사로 떨어져 지금과 같아진다 |
| ⚠️ 폰에서 편집 화면이 쓸 만한가 | 실기 미검증 — 41줄 textarea 의 터치감 |
| ❌ 브라우저 자동 확인 | Chrome 확장이 응답하지 않았다 (STATUS 에 적힌 것과 같은 상황) |

---

## 2026-09-03 (2) — 로고가 실제로 작아졌고, 앞으로는 저절로 갈아끼워진다

**한 줄**: 같은 날 (1) 이 **다른 자리를 고쳤다.** 진짜 원인을 잡고, 덤으로 재설치를
없앴다. (D-029 · D-030)

### 바뀐 것

**1. ⚠ (1) 의 진단이 틀렸다** — 시작 화면 로고는 여백 있는 `any` 아이콘이 아니라
**꽉 채운 `maskable` 아이콘**을 잘라 그린 것이었다. 스크린샷의 로고가 **스퀘어클**
모양인 것이 근거다. (1) 의 고침은 설치 대화상자 같은 다른 자리에서 여전히 맞지만,
**시작 화면은 손대지 못했다.**

**2. 주황 타일 크기는 못 줄인다** — 스크린샷에서 재 보니 **237dp**, 안드로이드 12+
시작 화면의 고정 규격 **240dp** 다. 큰 것은 타일이 아니라 **그 안의 체크**였다.

**3. 꽉 채운 아이콘의 체크를 72/108 만큼 미리 줄였다** — 마스크가 1.5배로 확대해
자르니 그만큼 되돌린 것이다.

| | 마스크 뒤, 타일 안에서 체크가 차지하는 폭 |
|---|---|
| 전 | **74%** (안드로이드 권장 67% 초과) |
| 후 | **48%** — 맥 아이콘·`any` 아이콘의 체크와 같은 비율 |

**4. 아이콘 주소에 지문이 붙는다** — `icon-512.png?v=e99bf374` 처럼. 그림이 바뀌면
주소가 반드시 바뀌므로 크롬이 갈아끼울 것을 알아본다. **지우고 다시 설치할 일이 없다.**
지문은 `tools/make-icons.py` 가 매니페스트에 직접 찍는다 — 사람이 기억할 일이 아니다.

**5. 서비스워커가 아이콘 이름을 더는 적지 않는다** — 매니페스트를 읽어 쓴다.
두 곳에 적으면 지문이 붙는 순간 한쪽이 낡고, 그건 오프라인에서만 드러난다.
`routine-v4` → **`routine-v5`**.

**6. 회귀 검사 9번이 생겼다** — 지문이 실제 파일과 맞는가, `any`·`maskable` 이 다
있는가, 서비스워커가 아이콘 이름을 다시 적지 않았는가. 어긋난 채 올라가면 기기에서만
조용히 옛 아이콘이 뜬다.

### 안 바뀐 것

- **본체 동작은 여전히 한 줄도 안 바뀌었다.** 앱 안에 로고 이미지는 없다
- **주황색·체크 모양·타일 크기** — 줄인 것은 체크의 크기 하나뿐이다
- 여백 있는 `icon-inset-*` 은 (1) 에서 만든 그대로다

### 확인한 것과 못 한 것

- ✅ 회귀 검사 **197건 + 9번 통과**
- ✅ 마스크를 흉내 내어 전/후를 그렸고, **전 그림이 스크린샷과 일치**했다 —
  흉내가 맞다는 근거다
- ✅ 마스크 뒤 체크 비율을 픽셀로 재서 74% → **48%** 확인
- ✅ 아이콘을 바꾸자 지문이 저절로 갱신되는 것을 확인 (`?v=d8defc70` → `?v=5a2952d6`)
- ⚠️ **안드로이드 실기에서 못 봤다.** 그리고 이번 것은 **자동 갱신을 기다려야**
  보인다 — 하루 안팎. 급하면 지우고 다시 설치하면 즉시 보인다

---

## 2026-09-03 — 시작 화면 로고를 적당한 크기로

**한 줄**: 안드로이드 시작 화면(스플래시)의 로고가 화면을 덮을 만큼 컸다.
여백을 가진 아이콘을 따로 만들어 그 자리에만 물렸다. (D-028)

### 바뀐 것

**1. 아이콘이 셋에서 다섯으로** — `tools/make-icons.py` 가 만드는 것

| 용도 | 파일 | 모양 |
|---|---|---|
| 파비콘 · iOS 홈 화면 · 안드로이드 홈 화면(`maskable`) | `icon-192.png`, `icon-512.png` | 꽉 채움 — **그대로** |
| **안드로이드 시작 화면**(매니페스트 `any`) | `icon-inset-192.png`, `icon-inset-512.png` | **새로 생김.** 824/1024 둥근 사각형 + 투명 여백 |
| 맥 독 | `mac/icon-mac-1024.png` | 같은 여백 도형 — **그대로** |

**2. 매니페스트가 `any` 와 `maskable` 을 다른 파일로 가리킨다** — 크롬은 홈 화면
아이콘에 `maskable`, 시작 화면에 `any` 를 쓴다. 따로 적어 두면 각 자리가 맞는 것을
집어간다.

**3. 서비스 워커** — `routine-v3` → **`routine-v4`**, 새 아이콘 둘을 캐시 목록에 넣었다.

### 안 바뀐 것

- **본체 동작은 한 줄도 안 바뀌었다.** 앱 안에는 로고 이미지가 아예 없다
  (시작 화면 좌상단의 `Routine` 은 글자다). 이 갱신은 껍데기만 만진다
- `icon-192.png` · `icon-512.png` · `mac/icon-mac-1024.png` 는 **바이트까지 같다.**
  맥 아이콘을 그리던 함수를 이름만 `inset_icon` 으로 바꿔 웹에도 같이 쓴 것이라,
  같은 입력에 같은 그림이 나온다
- 여백 비율은 맥과 같은 값(824/1024)을 그대로 가져다 썼다 — 새로 고른 값이 아니다

### 확인한 것과 못 한 것

- ✅ 회귀 검사 **197건 통과** (검사 항목은 늘지 않았다 — 아이콘은 검사 대상이 아니다)
- ✅ 매니페스트 JSON 파싱 통과
- ✅ 기존 세 아이콘이 바이트까지 그대로임을 `git diff` 로 확인
- ✅ 크림 바탕(`#fdfaf3`) 위에 전/후를 나란히 그려 눈으로 비교
- ⚠️ **안드로이드 실기에서 못 봤다.** 이미 설치된 앱은 **지우고 다시 설치해야**
  새 아이콘이 반영된다 — 이게 이 갱신의 유일한 미검증 항목이다

---

## 2026-08-28 (7) — 화면 톤이 기기 설정을 따른다

**한 줄**: 맥의 시스템 설정, 안드로이드의 다크 테마를 그대로 따라간다. (B-34, D-027)

### 바뀐 것

**1. `theme` 에 `'auto'` 가 생겼고, 루틴 넷 다 그것이다**

| 루틴 | 전 | 후 |
|---|---|---|
| Morning Routine | `light` | **`auto`** |
| Evening Routine | `dark` | **`auto`** |
| 수련과 운동 | `light` | **`auto`** |
| 출국 전 체크리스트 | `light` | **`auto`** |

`'light'` · `'dark'` 는 못박는 값으로 남아 있지만 **지금 쓰는 곳은 없다.**

**2. ⚠ 이브닝의 강제 어둡게가 빠졌다** — 이게 이 갱신에서 실제로 달라지는 부분이다.
기기를 종일 '밝게'로 두면 **이브닝 루틴도 밝게 뜬다.** 잠들기 전 밝은 화면을
앱이 막아 주던 것이 사라진다. 맥 야간 모드·안드로이드 다크 테마가 해질녘에
저절로 어두워지니 앱이 따로 판단할 필요가 없다고 본 것인데, **그 판단이 틀렸다면
`ROUTINES.evening.theme` 을 `'dark'` 로 되돌리면 된다 — 값 하나다.**

**3. 홈 화면 앱의 위쪽 띠 색도 따라간다** — 이건 CSS 가 못 정하고
`meta[theme-color]` 로 정해져서, JS 가 지금 톤을 계산해 넣는다.
**루틴 도중 기기 설정이 바뀌어도**(해질녘 자동 전환) 그때 다시 맞춘다.

**4. 어두운 색을 두 벌 적는다** — CSS 가 선택자와 미디어 질의를 한 규칙으로
못 묶는다. 한 벌로 끝나는 `light-dark()` 도 있지만 옛 안드로이드 웹뷰에서
통째로 무시될 수 있어 버렸다 (D-002 · 어디서 열어도 동작해야 한다).

**5. 회귀 검사** — 187건 → **197건**. 8번을 새로 뒀다.
**두 벌의 색이 글자 그대로 같은지**, CSS 와 JS 가 **같은 질의**를 보는지,
루틴 넷이 다 `auto` 인지를 본다. 일부러 한 글자를 어긋나게 해서
검사가 실제로 실패하는 것까지 확인했다.

### 안 바뀐 것

- 색 자체는 그대로다. 밝은 톤 · 어두운 톤의 값을 하나도 바꾸지 않았다
- 뷰 방식(`view`)·루틴 자리(`tier`)·항목 내용
- ⚠ `수련과 운동` 7개는 여전히 자리표시자다 (B-27)

### 확인한 것

로컬 서버 + 크롬. **이 맥이 지금 '어둡게'라서 그쪽이 진짜 기기 설정이다.**

| 확인한 것 | 결과 |
|---|---|
| 회귀 검사 | ✅ 197건 통과 / 실패 0 |
| 기기가 어둡게 → 시작 화면·모닝·이브닝 전부 어둡게 | ✅ |
| `light` 로 못박으면 밝게 뜨는가 | ✅ (못박는 값이 아직 살아 있다) |
| 위쪽 띠 색이 톤을 따라가는가 | ✅ |
| 두 벌의 색이 어긋나면 검사가 잡는가 | ✅ 일부러 틀려서 실패시켜 봤다 |
| 질의가 안 맞을 때 `auto` 가 밝게로 떨어지는가 | ✅ 질의를 뒤집은 **사본**으로 확인 |

### 확인 못 한 것 — 사람이 눌러야 한다

- **기기를 실제로 '밝게'로 두었을 때** — 위의 마지막 줄은 질의를 뒤집은 사본으로
  본 것이지 진짜 밝게 설정에서 본 것이 아니다. 맥 시스템 설정을 밝게로 바꿔
  한 번 열어 봐 주면 판정된다
- **기기가 해질녘에 저절로 어두워질 때 앱이 따라가는가** — 로직은 넣었고
  질의 변경 이벤트도 걸었지만, 실제로 넘어가는 순간을 보지 못했다
- **안드로이드 다크 테마** — 맥에서만 봤다
- 소리 · Wake Lock · 오프라인 · 맥앱 폴백 — 전부터 남아 있던 미검증

---

## 2026-08-28 (6) — push 를 묻지 않는다

**한 줄**: 일하는 방식이 바뀌었다. 앱은 그대로다. (B-33, D-026)

커밋했으면 **push 까지 한다.** `CLAUDE.md` 7번의 "원격 업로드는 반드시 먼저 묻는다"를
뒤집었다 — 이 앱은 여러 기기에서 같은 것을 보는 것이 전제라 **올라가지 않은 커밋은
되돌아갈 지점이 아니라 끝나지 않은 작업**이기 때문이다.

대신 **관문이 한 칸 앞으로 옮겨갔다.** 이 저장소는 Public 을 벗어날 수 없고
push 는 되돌릴 수 없는 공개다. 그래서 **공개되면 안 되는 것을 애초에 커밋하지
않는다** 가 지켜야 할 규칙이 됐다. 비밀번호·토큰·개인정보가 섞일 낌새가 있으면
커밋 전에 멈추고 묻는다.

**되돌리기·이력 삭제(`reset --hard` · `push --force` · 브랜치 삭제)는 그대로
먼저 묻는다.** push 여부와 다른 종류의 위험이다.

`index.html` 은 손대지 않았다. `VERSION` 도 그대로다.

---

## 2026-08-28 (5) — 출국 전 체크리스트에 실제 목록이 들어왔다

**한 줄**: 자리표시자 14개를 **사용자가 준 41개**로 통째로 바꿨다. (B-28)

### 바뀐 것

- `ROUTINES.departure.items` 전체 교체. **순서도 준 그대로** 두었다 —
  짐을 싸는 순서가 아니라 사용자가 적어 둔 순서에 뜻이 있을 수 있어서다
- 코드의 `⚠ 자리표시자` 주석을 떼고 출처(2026-08-28, 사용자)를 적었다
- 시작 화면(`☰` 안)의 표기가 `14개 항목` → `41개 항목`
- 회귀 검사 160건 → **187건**. 왕복·중복 검사가 항목 수를 따라 저절로 늘어난 것이다
  (D-023 에서 `Object.keys(ROUTINES)` 로 바꿔 둔 덕분에 검사 쪽은 손대지 않았다)

**설명(`|`)도 목표 시간(`@분`)도 붙이지 않았다.** 준 목록에 없는 것을 지어내지
않는다. 필요해지면 편집 화면에서 `이름 | 설명` 으로 덧붙이면 된다.

### 안 바뀐 것

- 뷰 방식은 그대로 `list` — 41개짜리라 오히려 한 화면에 훑는 것이 더 맞다 (D-024)
- ⚠ **`수련과 운동` 7개는 여전히 자리표시자다** (B-27)

### 확인한 것

| 확인한 것 | 결과 |
|---|---|
| 회귀 검사 | ✅ 187건 통과 / 실패 0 |
| 목록 41줄 · 첫(`코골이방지 테이프`)·끝(`패스트트랙`) 일치 | ✅ |
| 이름 중복 없음 | ✅ 검사 4번이 잡는다 |
| 형식을 깨는 글자(`|` · `@숫자`)가 섞였는가 | ✅ 없음 — 넣기 전에 확인했다 |
| 편집 화면 왕복 (텍스트로 뽑았다 되읽기) | ✅ 41줄 그대로 |
| 줄마다 완료 단추로 체크 (지갑·여권·환전) | ✅ 아래로 내려간다 |

### 확인 못 한 것

- **41줄을 폰에서 훑는 느낌** — 14줄일 때보다 훨씬 길어졌다. 스크롤이 편한지,
  끝낸 것이 아래로 쌓이면서 위쪽이 실제로 줄어드는 느낌이 나는지
- 목록 순서가 실제로 짐 싸기에 맞는지 — **준 순서 그대로 넣었을 뿐 판단하지 않았다**

---

## 2026-08-28 (4) — `☰` 화면 제목을 `Routines` 로

**한 줄**: 글자만 바꿨다. 동작은 그대로다.

`☰` 를 눌러 나오는 화면의 제목이 `가끔 하는 루틴` → **`Routines`** 가 됐다.
같은 화면을 여는 `☰` 단추의 이름(툴팁·읽어주기)도 함께 맞췄다 — 한쪽만 바꾸면
단추와 화면이 서로 다른 이름을 갖게 된다.

`ROUTINES` 의 `tier: 'more'` 는 그대로다. **자리를 나누는 기준은 안 바뀌었고
그 자리의 이름만 바뀐 것**이다 (D-023).

---

## 2026-08-28 (3) — 목록 뷰는 체크리스트가 됐다

**한 줄**: 목록 뷰에서 **순서와 무관하게 아무 줄이나** 완료할 수 있다.
끝낸 것은 아래로 내려가 흐려진다. (B-32, D-025)

### 바뀐 것

**1. 줄마다 `완료` 단추** — 이것을 누르는 것이 곧 완료다. 위에서부터 차례로
할 필요가 없다. 5번을 먼저 챙겼으면 5번을 누르면 된다.

**2. 끝낸 것은 아래로 내려간다** — 끝낸 순서대로 쌓이고 흐리게 뜬다.
그 앞에 `완료 4` 라고 적힌 구분선이 하나 들어간다. 위쪽에 남는 것이
**아직 안 한 것뿐**이라, 남은 개수를 세지 않아도 눈에 들어온다.

**3. 잘못 눌렀으면 `✓` 를 다시 누른다** — 끝낸 줄의 오른쪽 `✓` 가 취소 단추다.
`↩ 되돌리기` 는 마지막 하나만 되살리므로, 열네 줄을 훑다 엉뚱한 것을 눌렀을 때
쓸 것이 필요했다.

**4. 아래 단추의 뜻이 바뀌었다** (목록 뷰에서만)

| | 한 항목 뷰 | 목록 뷰 |
|---|---|---|
| 큰 단추 | `완료` (검은 큰 단추) | **`마치기`** — 작고 흐리게 |
| 왼쪽 | `← 이전` | `↩ 되돌리기` |
| 오른쪽 | `건너뛰기 →` | **없다** |
| 위쪽 숫자 | `7 / 27` | `3 / 14 · 5:12` |

`마치기` 를 **크고 검은 단추로 두지 않았다.** 진짜 조작이 줄마다 있는데 아래에
큰 단추가 있으면 그걸 눌러야 할 것처럼 보이고, 잘못 누르면 루틴이 끝나 버린다.
같은 이유로 목록 뷰에서는 `Enter`·`Space` 를 받지 않는다.

**5. 다 채우면 저절로 끝난다.** 채우지 못하고 `마치기` 를 누르면 남은 것이
요약에 **`안 함`** 으로 흐리게 남는다.

**6. 흐린 쪽이 뒤집혔다** — 전에는 아직 안 한 것이 흐렸는데(지금 항목을 돋보이게
하려고), 이제 끝낸 것이 흐리다. 목록 뷰의 주인공은 남은 일이다.
지금 항목을 검게 칠하던 강조도 없앴다 — 순서와 무관한 목록에서 하나를 강조하면
"이것부터 하라"가 되어 앞뒤가 안 맞는다.

**7. 안쪽 구조** — 기록이 스택에서 **끝낸 순서 목록**이 됐다.
`{ i, name, ms, skipped }` 로 항목 번호를 들고 다닌다. 예전에는 자리 번호가 곧
항목 번호였는데(순서대로만 끝냈으니까) 이제는 다르기 때문이다.
덕분에 **뷰를 오가도 진행이 이어진다** — 목록에서 5번만 체크하고 한 항목 뷰로
넘어가면 아직 안 한 1번이 뜬다.

**8. 회귀 검사** — 149건 → **160건**. 7번을 새로 뒀다. 줄 순서를 정하는
`listOrder()` 를 상태 없는 순수 함수로 빼서 검사가 직접 돌려본다.
**여기가 틀리면 훑는 도중 줄이 엉뚱한 데로 튀어 어디까지 했는지를 잃는다.**

### 안 바뀐 것

- **한 항목 뷰는 그대로다.** 완료·건너뛰기·이전·타이머·`@분` 알림 전부 예전 동작이다.
  모닝 루틴을 도는 경험은 하나도 안 바뀌었다
- **줄을 아무 데나 눌러도 완료되지는 않는다** — 단추라는 좁은 과녁을 일부러 남겼다.
  훑어보다 손이 스치는 것만으로 넘어가면 안 된다 (D-024 에서 정한 것)
- 기록·시간 재기·소리는 여전히 두 뷰가 **한 벌**을 쓴다
- ⚠ `수련과 운동` 7개와 `출국 전 체크리스트` 14개는 여전히 자리표시자다 (B-27·B-28)

### 확인한 것

로컬 서버(`python3 -m http.server 8742`) + 크롬.

| 확인한 것 | 결과 |
|---|---|
| 회귀 검사 | ✅ 160건 통과 / 실패 0 |
| 5 → 2 → 9 번을 순서 무시하고 체크 | ✅ 아래로 내려가 흐려지고, **누른 순서대로** 쌓인다 |
| 끝낸 줄의 `✓` 로 취소 | ✅ 제자리(2번)로 돌아온다 |
| `↩ 되돌리기` | ✅ 마지막에 끝낸 것 하나가 되살아난다 |
| 뷰를 오가도 진행이 이어지는가 | ✅ 목록에서 체크한 것은 한 항목 뷰에서 다시 안 나온다 |
| 전부 체크 | ✅ `마치기` 없이 저절로 요약 14줄 |
| 3개만 하고 `마치기` | ✅ 남은 11개가 `안 함` 으로 흐리게 |
| 목록을 다시 그려도 보던 자리를 지키는가 | ✅ 스크롤 위치 유지 |
| 모닝(순차)이 예전 그대로인가 | ✅ 완료 → 건너뛰기 → 이전, 타이머 정상 |

### 확인 못 한 것 — 사람이 눌러야 한다

- **`완료` 단추가 엄지로 눌리는 크기인가.** 40px 로 잡았고 줄 간격도 그에 맞춰
  줄였는데, **이 기능의 값어치가 정확히 여기 걸려 있다.** 판정을 못 했다
- **흐린 정도(42%)가 "끝난 것"으로 읽히는가** — 너무 흐려 안 보이는지,
  덜 흐려 구분이 안 되는지. 밝은 톤에서만 봤다
- **한 손으로 열네 줄을 훑을 때 오른쪽 단추까지 손이 닿는가**
- 소리 · Wake Lock · 오프라인 · 맥앱 폴백 — 전부터 남아 있던 미검증

---

## 2026-08-28 (2) — 루틴마다 보는 방식이 다르다

> 같은 날 두 번째 갱신이다. 버전은 날짜라 `VERSION` 은 그대로 `2026-08-28` 이다.
> "한 버전에 한 칸"과 어긋나지만, **이미 적은 칸은 고치지 않는다**는 쪽을 택했다.
> 하루에 두 번 바꾸면 이렇게 `(2)` 를 붙인다.

**한 줄**: 뷰 방식이 사용자 설정이 아니라 **루틴의 성질**이 됐다. 순서가 중요한
루틴은 한 항목씩, 순서와 무관한 체크리스트는 전체 목록으로 열린다. (B-31, D-024)

### 바뀐 것

**1. 루틴 메타에 `view` 가 붙었다** — `theme` 옆이다.

```js
departure: {
  label: '출국 전 체크리스트', short: '출국 전',
  tier:  'more',
  theme: 'light',
  view:  'list',            // ← 이번에 늘어난 것
  items: [ /* … */ ],
}
```

| 키 | 루틴 | 뷰 |
|---|---|---|
| `morning` | Morning Routine | `one` — 한 번에 한 항목 |
| `evening` | Evening Routine | `one` |
| `practice` | 수련과 운동 | `one` |
| `departure` | 출국 전 체크리스트 | **`list` — 열네 개가 한 화면에** |

**2. 목록 뷰가 생겼다** — 항목 화면에서 카드 대신 전체가 뜬다.

- 지금 할 것은 **채운 줄**로 뜨고 **늘 화면 가운데로 끌어온다**
- 지나온 것은 걸린 시간(또는 `건너뜀`), 아직인 것은 흐리게 + 목표 시간(`10분`)
- **줄을 눌러도 완료되지 않는다.** 완료는 큰 `완료` 버튼 하나뿐이다 — 훑어보다
  손이 스치는 것만으로 항목이 넘어가면 안 된다

**3. 실행 중 전환 버튼** — 항목 화면 오른쪽 위, 루틴 이름 옆.
`▤ 목록` / `▣ 하나씩` 으로 **누르면 무엇이 되는지**를 적는다. 맥에서는 `v` 키.

**4. 전환은 그 회차에만 걸린다 — 저장하지 않는다.**
`start()` 마다 루틴의 `view` 로 돌아간다. 처음엔 `localStorage` 에 저장했다가
**되돌렸다** — 체크리스트에서 목록으로 본 것이 다음 날 아침 모닝 루틴까지
따라오면 이 앱이 존재하는 이유가 조용히 무너진다 (D-024).

**5. 회귀 검사** — 127건 → **149건**. 메타 검사(5번)에 `view` 를 넣고 6번을 새로 뒀다.
6번이 못박는 것은 **순서가 중요한 셋이 `'one'` 이라는 것**과 **뷰가 저장되지 않는다는
것**이다. 이 기능의 실패 모습이 "목록이 순서가 중요한 루틴까지 번지는 것"이라서다.

**6. 곁가지** — 목록 안 자동 스크롤에서 `scrollIntoView` 와
`scrollTo({behavior:'smooth'})` 가 둘 다 통하지 않아(바깥이 `overflow:hidden`)
`scrollTop` 을 직접 계산한다. `VERSION` 과 `sw.js` 캐시 이름(`routine-v3`)은
그대로다 — 날짜가 같고, 서비스워커가 네트워크 우선이라 갱신이 막히지 않는다.

### 안 바뀐 것

- **제1 원칙** — 뒤집지 않았다. **어떤 루틴에 걸리는 원칙이었는지가 밝혀진 것**이다
  (D-024). 순서가 중요한 루틴에서는 여전히 다음 항목이 보이지 않는다
- **진행 로직** — 완료·건너뛰기·이전·시간 재기·소리·Wake Lock 을 두 뷰가 똑같이 쓴다.
  `render` · `tick` · `advance` · `back` 의 동작은 그대로다
- 모닝·이브닝의 항목 내용과 순서, 텍스트 형식(`이름 | 설명 @분`), 시작 화면·`☰`·
  편집 화면·요약 화면의 생김새
- ⚠ **`수련과 운동` 7개와 `출국 전 체크리스트` 14개는 여전히 자리표시자다** (B-27·B-28)

### 확인한 것

로컬 서버(`python3 -m http.server 8742`) + 크롬으로 눌러 확인했다.

| 확인한 것 | 결과 |
|---|---|
| 회귀 검사 | ✅ 149건 통과 / 실패 0 |
| 출국 전 체크리스트가 **목록으로 열림** (14줄) | ✅ |
| 모닝·이브닝은 한 항목으로 열림 | ✅ |
| 회차 전환이 다음 루틴에 안 따라옴 (출국 전↔모닝 왕복) | ✅ |
| 아무것도 저장되지 않음 (`localStorage` 가 비어 있다) | ✅ |
| 목록에서 완료·이전·건너뛰기 — 지나온 줄의 시간·`건너뜀` 표시 | ✅ |
| 지금 항목 자동 스크롤 (짧은 화면 380px 에서도 늘 보인다) | ✅ |
| 어두운 톤(이브닝)에서 지금 줄이 읽히는가 | ✅ |
| 끝까지 완주 → 요약 28줄 · `처음으로` | ✅ |
| 키보드 `v` 로 전환 | ✅ |

### 확인 못 한 것 — 사람이 눌러야 한다

- **실제 폰에서 목록을 훑는 느낌** — 줄 높이·글자 크기가 엄지로 훑기에 맞는지,
  `▤ 목록` 버튼이 오른쪽 위에서 눌리는지. **이 기능의 값어치가 걸린 지점인데
  판정을 못 했다**
- **`▤` · `▣` 글자가 안드로이드에서 제대로 뜨는가** — 맥 크롬에서만 봤다.
  안 뜨면 두부(□)로 보인다
- **소리** — 로직을 건드리지 않았으나 실기기 확인은 별개다
- **Wake Lock · 오프라인 실행 · 맥앱 오프라인 폴백** — 전부터 남아 있던 미검증 (B-22)
- **`file://` 에서 직접 열었을 때** — 로컬 서버로만 봤다

---

## 2026-08-28 — 루틴을 2개에서 4개로, 빈도별로 나눠 배치

**한 줄**: 루틴이 더 이상 모닝·이브닝 둘이 아니다. 자주 쓰는 것은 시작 화면에,
가끔 쓰는 것은 `☰` 뒤에 둔다. (B-27 · B-28 · B-29, D-023)

### 바뀐 것

**1. 루틴 자료구조에 메타가 붙었다** — 루틴 하나가 키 하나다.

```js
practice: {
  label: '수련과 운동',      // 화면에 크게 뜨는 이름
  short: '수련·운동',        // 좁은 자리 (진행 중 표시 · 편집 탭)
  tier:  'main',            // 'main' → 시작 화면 큰 버튼 / 'more' → ☰ 안
  theme: 'light',           // 이 루틴을 도는 동안의 화면 톤
  items: [ /* … */ ],
}
```

루틴을 하나 더 늘릴 때 **손댈 곳은 이 객체뿐**이다. 시작 화면·`☰` 목록·편집 탭·
회귀 검사가 전부 `ROUTINES` 를 읽어 스스로 늘어난다.

**2. 루틴 두 개가 늘었다**

| 키 | 이름 | 자리 | 톤 | 항목 |
|---|---|---|---|---|
| `morning` | Morning Routine | 시작 화면 | 밝게 | 27 |
| `evening` | Evening Routine | 시작 화면 | 어둡게 | 28 |
| `practice` | 수련과 운동 | 시작 화면 | 밝게 | **7 · ⚠자리표시자** |
| `departure` | 출국 전 체크리스트 | `☰` 안 | 밝게 | **14 · ⚠자리표시자** |

⚠ **새 루틴 두 개의 항목은 자리표시자다.** 실제 목록을 받으면 `items` 를 통째로
교체한다 (B-27 · B-28). 코드에도 같은 표시를 주석으로 달아 두었다.

**3. 시작 화면이 바뀌었다**

- 모닝/이브닝 **토글 + `시작` 버튼**이 없어졌다. 대신 **루틴마다 큰 버튼 하나**가
  뜨고, 누르면 바로 시작한다 — 고르기와 시작이 한 번의 조작이 됐다
- 시간대 제안(정오 경계)은 남았다. 다만 이제는 "선택해 둔 상태"가 아니라
  **제안된 하나만 채워서** 그린다. 예전 `시작` 버튼이 있던 자리를 그것이 대신한다
- 제안은 **화면을 그릴 때마다** 다시 계산한다. 정오를 넘겨 돌아왔는데 옛 제안이
  남아 있는 일이 없다
- 오른쪽 위에 **`☰`** — 가끔 하는 루틴이 여기 있다. 닫기는 `✕` 또는 `Esc`

**4. 화면 톤이 루틴에 붙었다** — `body[data-mode="evening"]` → `body[data-theme="dark"]`.
어두운 톤은 이제 "이브닝이라서"가 아니라 **그 루틴의 `theme` 이 `dark` 라서** 걸린다.

**5. 편집 화면이 루틴 개수를 따라간다** — 탭이 `ROUTINES` 에서 만들어지고(4개),
상태줄이 `출국 전 14개 · 전체 4개 루틴 76개` 처럼 전체를 함께 보여준다.
클로드코드 지시문도 `ROUTINES.<키> 의 items 를` 로 바뀌어 어느 루틴이든 가리킨다.

**6. 키보드 (맥)** — 시작 화면과 `☰` 화면에서 **숫자키 `1`·`2`·`3`…** 이 위에서부터
n 번째 루틴을 시작한다. `Enter`/`Space` 는 제안된 것, `Esc` 는 `☰` 닫기.

**7. 회귀 검사** — 79건 → **127건**. 두 가지가 늘었다.
- 왕복·중복 검사가 `["morning","evening"]` 고정에서 `Object.keys(ROUTINES)` 로 —
  **루틴을 늘리면 검사도 저절로 늘어난다**
- 새 검사 5번 · 루틴 메타(`label`·`short`·`tier`·`theme`·`items`)가 갖춰졌는지.
  메타를 빠뜨리면 시작 화면에 안 뜨거나 톤이 어긋나는데, 화면을 안 띄우고 잡을 수
  있는 것이 여기까지다

**8. 곁가지** — `sw.js` 캐시 이름 `routine-v2` → `v3`,
매니페스트 설명에서 "모닝·이브닝" 을 뺐다(더 이상 둘이 아니다), `VERSION` → `2026-08-28`.

### 안 바뀐 것

일부러 그대로 둔 것들이다.

- **제1 원칙** — 한 화면에 한 항목. 시작 화면과 `☰` 목록은 *루틴 이름*을 늘어놓는
  곳이지 *항목*을 미리 보여주는 곳이 아니다
- **모닝·이브닝의 항목 내용과 순서** (`CLAUDE.md` 4번 · 부분 수정 원칙)
- **텍스트 형식** — `이름 | 설명 @분`. 왕복 검사 전건 통과
- 진행 상태·기록을 저장하지 않는 것, `localStorage` 에 소리 설정만 두는 것 (D-005 · D-015)
- 항목 화면·요약 화면의 생김새와 동작

### 확인한 것

로컬 서버(`python3 -m http.server 8731`) + 크롬으로 눌러 확인했다.

| 확인한 것 | 결과 |
|---|---|
| 회귀 검사 | ✅ 127건 통과 / 실패 0 |
| 시작 화면에 큰 버튼 3개, 제안된 것만 채워짐 | ✅ |
| `☰` → 출국 전 체크리스트 한 개 · 눌러서 시작 | ✅ |
| 루틴별 톤 전환 (이브닝 어둡게 ↔ 나머지 밝게) | ✅ |
| 14개 끝까지 진행 → 요약에 `출국 전 체크리스트 완료` · 14줄 | ✅ |
| `홈` 으로 나갔다 돌아오기, 요약에서 `처음으로` | ✅ |
| 편집 탭 4개 · 상태줄 · 줄 번호 정렬 | ✅ |
| 키보드 `1`·`2`·`3` / `Enter` / `Esc` | ✅ |
| 화면이 짧을 때 목록 첫 항목이 잘리는가 | ✅ 안 잘린다 (`justify-content: safe center`) |

### 확인 못 한 것 — 사람이 눌러야 한다

- **실제 폰에서의 터치감** — 버튼이 커졌으니 오히려 나아졌을 것으로 보이지만
  **판정은 아니다.** 특히 `☰` 아이콘(48×48)이 엄지로 눌리는지
- **소리** — 로직을 건드리지 않았으므로 그대로일 것이나 실기기 확인은 별개다
- **Wake Lock · 오프라인 실행 · 맥앱 오프라인 폴백** — 전부터 남아 있던 미검증 (B-22)
- **넓은 화면 · 좁은 화면의 실제 레이아웃** — 창 크기를 실제로 줄여 보지는 못했다
  (`resize_window` 가 먹지 않아 `#app` 높이를 380px 로 줄여 대신 확인했다)

### 이 갱신에서 새로 생긴 문서

`docs/UPDATES.md` — 이 문서 (B-29).

---

## 2026-08-21 — 모닝 27개 · 홈 버튼

> 이 문서가 생기기 전의 갱신이다. `STATUS.md` 와 `DECISIONS.md` 에 적힌 것을
> 형식만 맞춰 옮겨 적었다. 당시에 이 문서는 없었다.

**한 줄**: 모닝 루틴 26 → 27개, 항목 화면에 `홈` 버튼. (B-24 · B-25 · B-26, D-022)

- 모닝에 `천부경신선법 음파축기공` 을 7번째로 추가. 이름 끝의 `(5분)` 은 글자가 아니라
  **5분 알림(`@5`)** 으로 넣었다
- 항목 화면 `← 이전` 과 `건너뛰기 →` **사이**에 `홈` — 실행 중 시작 화면으로 (D-022).
  확인 대화상자는 두지 않았다
- 편집 화면의 클로드코드 지시문이 로컬 경로 대신 **GitHub 저장소**를 가리킨다
- 확인: 홈 버튼 ✅(로컬 서버 + 크롬) · 27번째 항목 줄바꿈 ✅ · 터치 기기 ⚠️미확인

## 2026-08-17 — 처음 만들어 배포까지

**한 줄**: 설계부터 GitHub Pages 배포와 맥앱까지 하루에. (B-01~B-14 · B-21 · B-23,
D-001~D-021)

- 단일 HTML 본체 — 시작 → 항목 → 요약, 한 화면에 한 항목 (D-002)
- 실제 루틴 반영 (모닝 26 / 이브닝 28), WebAudio "띵" + 진동, 늘어나는 타이머
- 텍스트 편집 화면 + 줄 번호 (D-013), 회귀 검사 `tools/check.sh`
- GitHub Pages 배포, PWA 껍데기, 맥앱 `routine.app` (D-014 · D-016)
- 확인: 소리 ✅ · 안드로이드 설치 ✅ · 갱신 도달 ✅(D-017 로 한 번 실패한 뒤 해결)
