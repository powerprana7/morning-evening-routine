# 지금 상태 (STATUS)

> **세션을 시작하면 이 문서를 먼저 읽는다.** 여기 없는 건 `git log` 로 확인한다.
> 반대로, **동작이 바뀌는 커밋에서는 이 문서를 함께 갱신**한다.
> 이 문서는 "무엇이 되고 무엇이 안 되는가 + 다음에 뭘 할 것인가" 두 가지만 다룬다.
> 왜 그렇게 정했는지는 `DECISIONS.md`, 남은 후보 전체는 `BACKLOG.md` 로 간다.

**마지막 갱신**: 2026-08-21 · **버전**: `2026-08-21` (`index.html` 의 `VERSION`)

---

## 한 줄 요약

**돌아간다. 소리까지 확인됐다.**
https://powerprana7.github.io/morning-evening-routine/ · `routine.app`
가장 큰 리스크였던 소리와 **갱신 도달(D-017)** 이 둘 다 해소됐고, 안드로이드 설치·터치
분기도 확인됐다. 남은 미검증은 **오프라인 실행 · Wake Lock · 맥앱 오프라인 폴백** 셋.

## 구성요소별 상태

| 구성요소 | 위치 | 상태 |
|---|---|---|
| 본체 | `index.html` | **⚠️미검증** — 아래 "검증 상태" 참조 |
| 루틴 데이터 | 같은 파일 `ROUTINES` | **들어감** — 모닝 27 / 이브닝 28. 이브닝 순서는 ⚠️미확인 (D-010) |
| 편집 화면 | 같은 파일 | **동작** — 텍스트 편집 + 줄 번호. 내보내기/불러오기가 여기 흡수됨 (D-013) |
| 회귀 검사 | `tools/check.sh` | **동작** — 79건 통과. 의존성 0 (JavaScriptCore) |
| 배포 껍데기 | `manifest.webmanifest`, `sw.js`, `icon-*.png` | **작성됨 · ⚠️미검증** — `https` 에 올려봐야 판정된다 (D-014) |
| GitHub Pages | — | **배포됨** — https://powerprana7.github.io/morning-evening-routine/ (B-11) |

## 검증 상태 — 정확히 어디까지 확인했나

| 항목 | 상태 | 방법 |
|---|---|---|
| JS 문법 | ✅검증 (2026-08-17) | JavaScriptCore 로 `new Function(src)` 파싱 통과 |
| 시간 표기 `fmt()` | ✅검증 (2026-08-17) | 9개 경계값(0, 999ms, 59.4초, 1시간, 음수 등) 전부 통과 |
| 항목 수·중복 | ✅검증 (2026-08-21) | 모닝 27 / 이브닝 28, 각 목록 안에 이름 중복 없음 |
| 화면이 뜨는가 | ✅검증 (2026-08-17) | 맥에서 `file://` 로 열어 모닝 26개를 끝까지 통과. 스크린샷 확인 |
| 항목 진행·완료 화면 | ✅검증 (2026-08-17) | 26개 전부 기록되고 요약에 항목별 시간이 나옴 |
| **완료 화면 스크롤** | ✅검증→수정 (2026-08-17) | **스크롤 안 되는 버그 발견.** flex 자식 `min-height:0` 누락 + `#app` 높이 미확정. 고쳤으나 **수정본은 아직 ⚠️미확인** |
| **소리가 나는가** | ✅**검증 (2026-08-17)** | 사용자 확인. **가장 큰 리스크였고 해소됐다** |
| 텍스트 ↔ 목록 변환 | ✅검증 (2026-08-17) | 형식 12건 + 실제 루틴 54건 왕복. `tools/check.sh` |
| 되돌리기·건너뛰기 | ⚠️미검증 | B-05 |
| 편집 화면 줄 번호 정렬 | ✅검증 (2026-08-17) | 두 줄로 접히는 긴 항목에서도 어긋나지 않음 |
| 이브닝 루틴 통과 | ⚠️미검증 | 모닝만 통과했다 |
| **홈 버튼 (B-25)** | ✅검증 (2026-08-21) | 로컬 서버 + 크롬. 7/27 에서 눌러 시작 화면으로 복귀 확인. **터치 기기에서는 ⚠️미확인** |
| 27번째 항목 줄바꿈 | ✅검증 (2026-08-21) | 가장 긴 이름(음파축기공)이 3줄로 접히고 잘리지 않음. 넓은 화면 기준 |
| GitHub Pages 배포 | ✅검증 (2026-08-17) | 안드로이드 크롬에서 접속 확인 |
| **배포본이 기기에 도달하는가** | ✅검증 (2026-08-17) | 두 번째 push(모닝 26개)도 도달. 첫 방문이 아니어도 갱신된다는 뜻이라 **서비스워커 캐시 갱신까지 확인됨** |
| 안드로이드 홈 화면 설치 | ✅검증 (2026-08-17) | `Routine` 이름으로 설치됨 |
| 터치 기기에서 편집 버튼 감춤 | ✅검증 (2026-08-17) | 안드로이드에서 사라진 것 확인 |
| **오프라인 실행** | ⚠️미검증 | 비행기 모드에서 열리는지 |
| **자동 갱신 (D-017)** | ✅**검증 (2026-08-17)** | **판정 완료.** 모닝 26개 push 후 안드로이드에서 손대지 않고 새 목록이 도달함 (사용자 확인) |
| Wake Lock (화면 꺼짐 방지) | ⚠️미검증 | 루틴 중 화면이 안 꺼지는지 |
| 맥앱 오프라인 폴백 | ⚠️미검증 | 네트워크 끊고 `routine.app` 실행 |

**브라우저 자동 조작은 못 했다** — Chrome 확장이 연결돼 있지 않았고, `file://` 은
확장이 열지도 못한다. 그래서 위 셋은 사람이 눌러야만 판정된다.

## 지금 진행 중

없음. **2026-08-21 갱신 완료** — 모닝 27개 · 홈 버튼 · 지시문 대상 변경 (B-24~B-26, D-022).

**2026-08-21 에 바뀐 것**
- 모닝 루틴 **26 → 27개**. `천부경신선법 음파축기공` 을 `천부경, 수진선학 훈` 뒤(7번째)에 추가
- 항목 화면 `← 이전` 과 `건너뛰기 →` 사이에 **`홈`** — 실행 중 시작 화면으로 (D-022)
- 편집 화면의 클로드코드 지시문이 로컬 경로 대신 **GitHub 저장소**를 가리킨다 (D-022)

## 다음에 할 일 — 상위 3개

**급한 것은 없다. 며칠 실제로 써 보는 것이 다음 단계다.**

1. **B-16 · 이브닝 순서** — 사용자가 직접 조정하겠다고 함 (2026-08-17). 텍스트를 주면 반영
2. **B-19 · 시간 알림 항목 정하기** — 지금은 `오일풀링 @10` 하나뿐.
   27번째 항목의 `(5분)` 은 이름에 적힌 글자일 뿐 알림이 아니다 — 알림으로 바꾸려면 `@5`
3. **B-18 · 병렬 항목** — 마스크팩·오일풀링이 실제로 거슬리는지 며칠 써 보고 판단 (D-012)

## 가장 큰 리스크

**소리도, 갱신 도달도 해소됐다.** 큰 리스크는 남아 있지 않다.

갱신 문제는 실제로 한 번 당했었다 — push 한 수정이 안드로이드에 도달하지 않고, 서버에는
새 파일이 있는데 기기만 옛 화면을 보고 있었다(D-017). 조용히 실패하는 종류라 위험했지만,
2026-08-17 모닝 26개 push 에서 **손대지 않아도 새 목록이 도달하는 것을 확인**해 판정이
끝났다. 첫 방문이 아니었으므로 서비스워커 캐시 갱신까지 함께 검증된 셈이다.

남은 미검증은 **오프라인 실행 · Wake Lock · 맥앱 오프라인 폴백** 셋인데, 셋 다 실패해도
평소 사용에는 지장이 없다.

**교훈 하나**: 완료 화면 스크롤 버그는 정적 검사(문법·단위 테스트)를 전부 통과한
상태에서 **사람이 한 번 눌러보자마자** 나왔다. 레이아웃은 자동 검사가 못 잡는다는
증거다 — B-06 회귀 검사를 만들 때 이 영역을 "검사가 못 하는 것"에 명시한다.

## 여기까지 온 흐름

| 단계 | 시기 | 내용 |
|---|---|---|
| 1 · 설계 토의 | 2026-08-17 | 실행 방식·항목 관리·타이머·기록 여부 결정 (D-001~D-008) |
| 2 · 형식 확정 | 2026-08-17 | HTML 로 확정, 예약 알림 불필요 확인 (D-009) |
| 3 · 본체 작성 | 2026-08-17 | 실제 루틴 반영 + 앱 작성 (D-010~D-012, B-01~B-04·B-09) |
| 4 · 편집·검사 | 2026-08-17 | 텍스트 편집 화면 + 줄 번호 + 회귀 검사 (D-013, B-06~B-08) |
| 5 · 배포 준비 | 2026-08-17 | `index.html` 로 이동, PWA 껍데기, GitHub Pages 결정 (D-014) |
| 6 · 배포·맥앱 | 2026-08-17 | Pages 공개, 앱이 루틴을 저장하지 않도록 정리, `routine.app` (D-015·D-016) |
| 7 · 실기기 확인 | 2026-08-17 | 소리·줄번호·안드로이드 설치 확인, 캐시로 갱신이 막히던 문제 수정 (D-017) |
| 8 · 실사용 갱신 | 2026-08-21 | 모닝 27개, 홈 버튼, 지시문 대상을 GitHub 으로 (B-24~B-26, D-022) |

## 알려진 함정

- **외부 파일을 `fetch` 로 읽으면 `file://` 에서 통째로 죽는다** — CORS (D-002)
- **`setInterval` 로 시간을 누적하면 백그라운드에서 밀린다** — `Date.now()` 차이로 (D-004)
- **localStorage 는 출처마다 따로 논다** — `file://` 과 `https://` 는 다른 저장소다.
  배포 방식을 바꾸면 편집분이 사라진다. B-11 전에 B-08 이 먼저다
- **Chrome 확장은 `file://` 을 못 연다** — 브라우저 자동 확인을 하려면 로컬 서버로
  띄워야 한다: `python3 -m http.server 8731`. 다만 그건 `file://` 검증이 아니다.
  최종 확인은 사람이 파일을 직접 열어서 한다
- **서비스워커·매니페스트는 `file://` 에서 안 붙는다** — 맥에서 더블클릭해 여는 동안은
  아무 일도 일어나지 않는 것이 정상이다. `https` 에 올라가야 판정된다
- **앱 편집은 그 기기에만 남는다** — 전 기기 반영은 원본을 고쳐 push 해야 한다 (D-014)
- **모바일 시뮬레이터는 터치감·소리·Wake Lock 을 판정하지 못한다** — 셋 다 실기기 전용
- **설치한 PWA 는 주소창도 새로고침 버튼도 없다** — 앱 전환기에서 다시 열어도 새로
  받아오지 않는다. "서버에 올렸으니 됐다"고 착각하기 딱 좋은 지점이다 (D-017)
- **GitHub Pages 는 `max-age=600` 을 보낸다** — 서비스워커가 네트워크 우선이어도
  브라우저 HTTP 캐시가 10분간 낡은 응답을 준다. `cache: 'reload'` 로 우회했다
- **매니페스트의 앱 이름은 이미 설치된 아이콘에 반영되지 않는다** — 지우고 다시 설치해야 한다
